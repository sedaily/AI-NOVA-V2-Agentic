# Agentic-Nexus: 기자용 통합 AI 작업 플랫폼

---

## 1. 프로젝트 개요

### 프로젝트명
Agentic-Nexus (에이전틱 넥서스)

### 목표
기자들이 업무를 효율적으로 처리할 수 있는 Agentic AI 기반 통합 작업 플랫폼 개발

### 핵심 전환
```
현재: 도구 기반 (Tool-based)
      기자가 6개 도구 중 선택 → 각각 실행 → 결과 조합

목표: 에이전트 기반 (Agent-based)
      기자가 자연어로 요청 → AI가 판단 → 자동 실행/조합
```

---

## 2. 현재 상태 (As-Is)

### 완료된 작업

#### Phase 1: 버디 서비스 아키텍처 구축
- 6개 백엔드 서비스 분리 구성
  - b1 (buddy): 일보버디, 기사버디
  - t1 (title): 제목생성_5종, 제목창의_7종
  - p1 (proofreading): 교열_경제분야, 교열_사회분야
  - f1 (foreign): 외신_영어, 외신_일어
  - w1 (bodo): 보도자료_기업, 보도자료_공공
  - r1 (regression): 퇴고_단문, 퇴고_장문

- 프론트엔드 버디 설정 파일 생성
  - `buddyServiceConfig.js`: SERVICE_ENDPOINTS, BUDDY_CONFIG 정의
  - 각 버디별 serviceCode, promptTable, promptType, engineType 매핑

#### Phase 2: WebSocket 라우팅 구현
- 버디 타입에 따른 동적 WebSocket 엔드포인트 연결
- `getServiceEndpoint()` 함수로 버디별 REST/WS 엔드포인트 반환
- `reconnectForBuddy()` 함수로 서비스 변경 시 WebSocket 재연결

#### Phase 3: Claude API 통합
- AWS Bedrock → Claude 4.5 Opus Direct API 마이그레이션
- Anthropic API 직접 연동 (`claudeService.js`)
- 듀얼 AI 프로바이더 지원 (Anthropic + Bedrock 폴백)

#### Phase 4: 프롬프트 연결 버그 수정
- 문제: 버디 버튼 클릭 시 해당 프롬프트가 적용되지 않음
- 원인 1: t1 서비스 DynamoDB 프롬프트 ID 불일치 (`'11'` → `'C7'`)
- 원인 2: ChatPage.jsx에서 `selectedEngine` 고정값 사용
- 해결: `buddyService?.engineType || selectedEngine` 로직 적용

#### Phase 5: 배포
- Vite 빌드 → S3 업로드 → CloudFront 캐시 무효화
- 배포 URL: `d64sb1awx45dl.cloudfront.net`

### 현재 기술 스택
| 영역 | 기술 |
|------|------|
| Frontend | React, Vite, TailwindCSS |
| Backend | AWS Lambda, API Gateway (REST/WebSocket) |
| AI | Claude 4.5 Opus (Anthropic API), AWS Bedrock (폴백) |
| Database | DynamoDB (프롬프트, 대화, 사용량) |
| 배포 | S3, CloudFront |
| 인프라 | AWS (us-east-1) |

---

## 3. 문제 정의

### 현재 서비스의 한계

1. **접근성 제한**
   - 6개 서비스가 각각 분리되어 있음
   - 기자가 여러 창에서 작업해야 함

2. **비용 증가**
   - 프롬프트 중복으로 토큰량 증가
   - 각 서비스마다 스타일북/문체 규칙 반복 포함

3. **비효율적 워크플로우**
   - 기자가 도구를 직접 선택해야 함
   - 작업 결과를 수동으로 다음 도구에 전달

---

## 4. 목표 상태 (To-Be)

### Agentic AI 플랫폼 구조

```
┌─────────────────────────────────────────────────────┐
│                    사용자 인터페이스                  │
│         "삼성전자 실적 기사 완성해줘"                 │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│              Router Agent (라우터)                   │
│         의도 분석 → 도구 선택 → 실행 순서 결정        │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│                  Tool Layer (도구)                   │
├─────────┬─────────┬─────────┬─────────┬─────────────┤
│ 기사작성 │ 제목생성 │   교열   │ 외신번역 │ 보도자료변환│
└─────────┴─────────┴─────────┴─────────┴─────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│              Common Module (공통 모듈)               │
│       스타일북 │ 문체 가이드 │ 경제 용어 사전         │
└─────────────────────────────────────────────────────┘
```

---

## 5. Agentic AI 도입 이점

### 5.1 사용자(기자) 관점

| 현재 (도구 기반) | Agentic AI 적용 후 |
|------------------|-------------------|
| 도구 선택 → 입력 → 결과 복사 → 다음 도구 | 한 마디로 전체 작업 완료 |
| 6개 도구 사용법 학습 필요 | 자연어로 통일 |
| 중간 결과물 직접 전달 | 자동 체이닝 |
| 도구마다 다른 인터페이스 | 단일 화면 |

**핵심: "도구를 쓰는 것"에서 "일을 시키는 것"으로 전환**

### 5.2 비용 관점

```
현재 구조:
  제목생성 호출: 15,000 토큰
  교열 호출:     15,000 토큰
  합계:          30,000 토큰 (프롬프트 중복 포함)

Agentic 구조:
  공통 모듈:     10,000 토큰 (1회)
  제목 로직:      3,000 토큰
  교열 로직:      3,000 토큰
  합계:          16,000 토큰

  절감율: 약 47%
```

### 5.3 기술/운영 관점

| 항목 | 이점 |
|------|------|
| 확장성 | 새 기능 = Tool 하나 추가, 라우터가 자동 활용 |
| 유지보수 | 공통 모듈 1곳 수정 → 전체 반영 |
| 일관성 | 스타일북/문체 한 번 정의 → 모든 도구에 적용 |
| 모니터링 | 워크플로우 단위 추적 가능 |

### 5.4 차별화 관점

```
일반 AI 서비스          Agentic AI 플랫폼
─────────────────────────────────────────────
"챗봇"                  "AI 동료"
질문-응답               작업 위임
단발성                  연속 작업
수동 조합               자동 조합
```

### 5.5 기대 효과 요약

- **접근성 개선:** 6개 페이지 → 1개 화면
- **비용 절감:** 프롬프트 중복 제거로 토큰 사용량 40-50% 감소
- **작업 시간 단축:** 도구 선택/전환 없이 한 번의 요청으로 완결
- **확장성 확보:** 신규 기능 추가 시 도구만 추가하면 에이전트가 자동 활용

---

## 6. 구현 방법

### 6.1 사용자 흐름에 맞는 통합 인터페이스 설계

기자가 하나의 화면에서 벗어나지 않고 모든 작업을 수행할 수 있는 통합 인터페이스 설계. 기자가 도구를 선택하는 방식이 아니라, 작업 의도를 입력하면 시스템이 적절한 기능을 실행하는 구조.

```
현재: 버디 버튼 6개 + 채팅창
     ┌──────────────────────────────────┐
     │  [일보] [제목] [교열] [외신]...  │ ← 버튼 선택 필요
     │  ┌─────────────────────────────┐ │
     │  │        채팅 영역            │ │
     │  └─────────────────────────────┘ │
     └──────────────────────────────────┘

전환: 의도 기반 단일 입력
     ┌──────────────────────────────────┐
     │  "삼성전자 실적 기사 써줘"       │ ← 자연어 입력
     │  ┌─────────────────────────────┐ │
     │  │ 🔄 취재 → 작성 → 제목 → 교열 │ │ ← 워크플로우 표시
     │  └─────────────────────────────┘ │
     │  ┌─────────────────────────────┐ │
     │  │        결과물 영역          │ │
     │  └─────────────────────────────┘ │
     └──────────────────────────────────┘
```

### 6.2 프롬프트 공통 모듈 추출

각 서비스에 중복 존재하는 규칙(문체, 스타일북, 용어 등)을 공통 모듈로 분리하고, 기능별 핵심 지침만 남김.

```
현재 상태:
  일보버디:  [스타일북] + [문체] + [일보 로직]
  제목생성:  [스타일북] + [문체] + [제목 로직]
  교열:      [스타일북] + [문체] + [교열 로직]
  ...

목표 상태:
  공통 모듈 (1회 로드):
    ├─ 서울경제 스타일북
    ├─ 문체 가이드
    └─ 경제 용어 사전

  기능 모듈 (필요시 호출):
    ├─ 제목생성 로직만
    ├─ 교열 로직만
    └─ ...
```

### 6.3 Tool 형태 API 전환

각 기능을 독립된 API(AWS Lambda)로 만들어, 에이전트가 필요에 따라 호출할 수 있게 구성.

```
/agentic-nexus-tools/
├─ common/
│   ├─ stylebook_loader.py      # 공통 모듈 로더
│   └─ token_optimizer.py       # 토큰 최적화
│
├─ tools/
│   ├─ title_generator/         # 제목 생성 도구
│   ├─ proofreader/             # 교열 도구
│   ├─ article_writer/          # 기사 작성 도구
│   ├─ foreign_translator/      # 외신 번역 도구
│   ├─ press_converter/         # 보도자료 변환 도구
│   └─ revision_editor/         # 퇴고 도구
│
└─ router/
    └─ agent_router.py          # Bedrock Agent 연동
```

### 6.4 라우터 에이전트 개발

기자의 자연어 입력을 분석해 어떤 도구를 어떤 순서로 실행할지 자동 판단하는 에이전트. AWS Bedrock Agents 기반 구축.

```
┌─────────────────────────────────────────────────────┐
│              Bedrock Agent (라우터)                  │
├─────────────────────────────────────────────────────┤
│ Foundation Model: Claude 3.5 Sonnet / Opus          │
│                                                      │
│ Action Groups:                                       │
│   ├─ 기사작성 (article_writer)                      │
│   ├─ 제목생성 (title_generator)                     │
│   ├─ 교열검수 (proofreader)                         │
│   ├─ 외신번역 (foreign_translator)                  │
│   ├─ 보도자료변환 (press_release_converter)         │
│   └─ 퇴고수정 (revision_editor)                     │
│                                                      │
│ Knowledge Base:                                      │
│   └─ 서울경제 스타일북 (S3 + OpenSearch)            │
└─────────────────────────────────────────────────────┘
```

### 6.5 파일럿 검증

소수 기자 대상으로 기존 개별 도구 대비 품질/속도/편의성을 비교 검증.

| 지표 | 측정 방법 | 목표 |
|------|-----------|------|
| 작업 완료 시간 | 기존 대비 소요 시간 | 50% 단축 |
| 도구 전환 횟수 | 페이지 이동 수 | 0회 |
| 토큰 사용량 | API 로그 분석 | 40% 절감 |
| 품질 만족도 | 기자 설문 (1-5점) | 4.0 이상 |
| 라우터 정확도 | 의도 매칭률 | 90% 이상 |

---

## 7. 로드맵

### Phase 1: 기반 작업
- [ ] 6개 서비스 프롬프트 분석
- [ ] 공통 모듈 / 기능 모듈 분리 설계
- [ ] DynamoDB 테이블 재설계

### Phase 2: Tool 전환
- [ ] Lambda 함수 6개 개발 (Tool 형태)
- [ ] Tool 스키마 정의 (Bedrock Agent용)
- [ ] 개별 도구 테스트

### Phase 3: 라우터 개발
- [ ] AWS Bedrock Agent 설정
- [ ] 라우터 의도 분류 로직 개발
- [ ] 워크플로우 체이닝 구현

### Phase 4: UI 통합
- [ ] 프론트엔드 단일 인터페이스 개발
- [ ] 워크플로우 시각화 컴포넌트
- [ ] 실시간 진행 상태 표시

### Phase 5: 검증 및 최적화
- [ ] 내부 알파 테스트
- [ ] 기자 베타 테스트 (3-5명)
- [ ] 피드백 반영 및 최적화
- [ ] 전체 오픈

---

## 8. 기술 구현 상세

### AWS 서비스 활용 계획

| 서비스 | 용도 |
|--------|------|
| Bedrock Agents | 라우터 에이전트 |
| Bedrock Knowledge Base | 스타일북 RAG |
| Lambda | Tool 함수 실행 |
| API Gateway | REST/WebSocket API |
| DynamoDB | 프롬프트, 대화, 사용량 저장 |
| S3 | 정적 파일, Knowledge Base 소스 |
| CloudFront | CDN |
| OpenSearch | Knowledge Base 벡터 검색 |

### 아키텍처 다이어그램

```
[사용자]
    ↓ WebSocket
[API Gateway]
    ↓
[Lambda: Router Handler]
    ↓
[Bedrock Agent]
    ├─→ [Knowledge Base] ← [S3: 스타일북]
    │
    └─→ [Action Groups]
            ├─→ [Lambda: title_generator]
            ├─→ [Lambda: proofreader]
            ├─→ [Lambda: article_writer]
            └─→ ...
                    ↓
            [DynamoDB: prompt-modules]
                    ↓
            [Bedrock/Anthropic API]
                    ↓
            [응답 스트리밍]
```

---

## 9. 참고 사항

### AWS 지원 어필 포인트
- Bedrock Agents 실사용 사례 (언론사 최초)
- 토큰 최적화 아키텍처
- 멀티 에이전트 협업 구조

### 리스크 및 대응
| 리스크 | 대응 방안 |
|--------|-----------|
| 라우터 판단 오류 | 기존 버디 버튼 방식 폴백 제공 |
| Bedrock Agent 지연 | 캐싱 및 비동기 처리 |
| 프롬프트 마이그레이션 품질 | 단계적 전환, A/B 테스트 |

---

## 10. 문서 이력

| 버전 | 내용 |
|------|------|
| v1.0 | 초기 작성 - 프로젝트 개요 및 로드맵 |

---

*본 문서는 Agentic-Nexus 프로젝트의 기획 및 구현 가이드입니다.*
